name: Deploy to Cloud Run

on:
  push:
    branches:
      - develop
      - staging
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - develop
      - staging
  workflow_dispatch:
    inputs:
      branch:
        description: Rama a desplegar (se despliega en dev)
        required: true
        type: string
      schema_override:
        description: 'Schema override (opcional, ej: custom_schema)'
        required: false
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    env:
      DEPLOY_ENV: ""
      IMAGE_TAG: ""
      IMAGE_URI: ""
    environment: ${{ github.event_name == 'workflow_dispatch' && vars.DEPLOY_ENV_DEV || github.event_name == 'pull_request' && vars.DEPLOY_ENV_DEV || github.ref_name == 'develop' && vars.DEPLOY_ENV_DEV || github.ref_name == 'staging' && vars.DEPLOY_ENV_STG || github.ref_name == 'main' && vars.DEPLOY_ENV_PROD }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.event.pull_request.head.ref || github.ref_name }}

      - name: Set deploy variables
        env:
          BRANCH_INPUT: ${{ github.event.inputs.branch }}
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          GCP_REGION: ${{ vars.GCP_REGION }}
          GCP_PROJECT_ID_DEV: ${{ vars.GCP_PROJECT_ID_DEV }}
          GCP_PROJECT_ID_PROD: ${{ vars.GCP_PROJECT_ID_PROD }}
          ARTIFACT_REGISTRY: ${{ vars.ARTIFACT_REGISTRY }}
          IMAGE_NAME: ${{ vars.IMAGE_NAME }}
          SERVICE_NAME_DEV: ${{ vars.SERVICE_NAME_DEV }}
          SERVICE_NAME_PROD: ${{ vars.SERVICE_NAME_PROD }}
          CLOUD_RUN_SERVICE_ACCOUNT_DEV: ${{ vars.CLOUD_RUN_SERVICE_ACCOUNT_DEV }}
          CLOUD_RUN_SERVICE_ACCOUNT_PROD: ${{ vars.CLOUD_RUN_SERVICE_ACCOUNT_PROD }}
          WIF_PROVIDER_DEV: ${{ vars.WIF_PROVIDER_DEV }}
          WIF_PROVIDER_PROD: ${{ vars.WIF_PROVIDER_PROD }}
          WIF_SERVICE_ACCOUNT_DEV: ${{ vars.WIF_SERVICE_ACCOUNT_DEV }}
          WIF_SERVICE_ACCOUNT_PROD: ${{ vars.WIF_SERVICE_ACCOUNT_PROD }}
        run: |
          ref="${GITHUB_REF_NAME}"
          deploy_env=""
          image_tag=""
          gcp_project_id=""
          service_name=""
          cloud_run_sa=""
          wif_provider=""
          wif_sa=""
          
          if [ -z "$GCP_REGION" ]; then
            echo "Missing GCP_REGION variable." >&2
            exit 1
          fi
          if [ -z "$ARTIFACT_REGISTRY" ]; then
            echo "Missing ARTIFACT_REGISTRY variable." >&2
            exit 1
          fi
          if [ -z "$IMAGE_NAME" ]; then
            echo "Missing IMAGE_NAME variable." >&2
            exit 1
          fi
          
          # Pull Request: siempre deployar a DEV
          if [ "$EVENT_NAME" = "pull_request" ]; then
            # Validar que el PR apunta a develop o staging
            if [ "$PR_BASE_REF" != "develop" ] && [ "$PR_BASE_REF" != "staging" ]; then
              echo "PRs can only target develop or staging branches" >&2
              exit 1
            fi
            deploy_env="${{ vars.DEPLOY_ENV_DEV }}"
            image_tag="${{ vars.IMAGE_TAG_DEV }}"
            gcp_project_id="$GCP_PROJECT_ID_DEV"
            service_name="$SERVICE_NAME_DEV"
            cloud_run_sa="$CLOUD_RUN_SERVICE_ACCOUNT_DEV"
            wif_provider="$WIF_PROVIDER_DEV"
            wif_sa="$WIF_SERVICE_ACCOUNT_DEV"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            if [ -z "$BRANCH_INPUT" ]; then
              echo "Missing required inputs" >&2
              exit 1
            fi
            deploy_env="${{ vars.DEPLOY_ENV_DEV }}"
            image_tag="${{ vars.IMAGE_TAG_DEV }}"
            gcp_project_id="$GCP_PROJECT_ID_DEV"
            service_name="$SERVICE_NAME_DEV"
            cloud_run_sa="$CLOUD_RUN_SERVICE_ACCOUNT_DEV"
            wif_provider="$WIF_PROVIDER_DEV"
            wif_sa="$WIF_SERVICE_ACCOUNT_DEV"
          else
            if [ "$ref" = "develop" ]; then
              deploy_env="${{ vars.DEPLOY_ENV_DEV }}"
              image_tag="${{ vars.IMAGE_TAG_DEV }}"
              gcp_project_id="$GCP_PROJECT_ID_DEV"
              service_name="$SERVICE_NAME_DEV"
              cloud_run_sa="$CLOUD_RUN_SERVICE_ACCOUNT_DEV"
              wif_provider="$WIF_PROVIDER_DEV"
              wif_sa="$WIF_SERVICE_ACCOUNT_DEV"
            elif [ "$ref" = "staging" ]; then
              deploy_env="${{ vars.DEPLOY_ENV_STG }}"
              image_tag="${{ vars.IMAGE_TAG_STG }}"
              # Staging usa dev por ahora (puede cambiarse después)
              gcp_project_id="$GCP_PROJECT_ID_DEV"
              service_name="$SERVICE_NAME_DEV"
              cloud_run_sa="$CLOUD_RUN_SERVICE_ACCOUNT_DEV"
              wif_provider="$WIF_PROVIDER_DEV"
              wif_sa="$WIF_SERVICE_ACCOUNT_DEV"
            elif [ "$ref" = "main" ]; then
              deploy_env="${{ vars.DEPLOY_ENV_PROD }}"
              image_tag="${{ vars.IMAGE_TAG_PROD }}"
              gcp_project_id="$GCP_PROJECT_ID_PROD"
              service_name="$SERVICE_NAME_PROD"
              cloud_run_sa="$CLOUD_RUN_SERVICE_ACCOUNT_PROD"
              wif_provider="$WIF_PROVIDER_PROD"
              wif_sa="$WIF_SERVICE_ACCOUNT_PROD"
            else
              echo "Unsupported branch: $ref" >&2
              exit 1
            fi
          fi
          
          if [ -z "$gcp_project_id" ]; then
            echo "Missing GCP_PROJECT_ID for environment: $deploy_env" >&2
            exit 1
          fi
          if [ -z "$service_name" ]; then
            echo "Missing SERVICE_NAME for environment: $deploy_env" >&2
            exit 1
          fi
          if [ -z "$cloud_run_sa" ]; then
            echo "Missing CLOUD_RUN_SERVICE_ACCOUNT for environment: $deploy_env" >&2
            exit 1
          fi
          if [ -z "$wif_provider" ]; then
            echo "Missing WIF_PROVIDER for environment: $deploy_env" >&2
            exit 1
          fi
          if [ -z "$wif_sa" ]; then
            echo "Missing WIF_SERVICE_ACCOUNT for environment: $deploy_env" >&2
            exit 1
          fi

          printf 'DEPLOY_ENV=%s\n' "$deploy_env" >> "$GITHUB_ENV"
          printf 'IMAGE_TAG=%s\n' "$image_tag" >> "$GITHUB_ENV"
          printf 'GCP_PROJECT_ID=%s\n' "$gcp_project_id" >> "$GITHUB_ENV"
          printf 'SERVICE_NAME=%s\n' "$service_name" >> "$GITHUB_ENV"
          printf 'CLOUD_RUN_SERVICE_ACCOUNT=%s\n' "$cloud_run_sa" >> "$GITHUB_ENV"
          printf 'WIF_PROVIDER=%s\n' "$wif_provider" >> "$GITHUB_ENV"
          printf 'WIF_SERVICE_ACCOUNT=%s\n' "$wif_sa" >> "$GITHUB_ENV"

      - name: Set DB_SCHEMA for branch isolation
        id: set_schema
        env:
          SCHEMA_OVERRIDE: ${{ github.event.inputs.schema_override }}
        run: |
          # Si hay schema_override manual, usarlo directamente
          if [ -n "$SCHEMA_OVERRIDE" ]; then
            DB_SCHEMA="$SCHEMA_OVERRIDE"
            echo "schema=${DB_SCHEMA}" >> "$GITHUB_OUTPUT"
            echo "DB_SCHEMA=${DB_SCHEMA}" >> "$GITHUB_ENV"
            echo "DB_SCHEMA set to override: $DB_SCHEMA"
            exit 0
          fi
          
          # Para PRs: usar pr_<number>
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            PR_NUMBER="${{ github.event.pull_request.number }}"
            DB_SCHEMA="pr_${PR_NUMBER}"
            echo "schema=pr_${PR_NUMBER}" >> "$GITHUB_OUTPUT"
            echo "DB_SCHEMA=pr_${PR_NUMBER}" >> "$GITHUB_ENV"
          # Para develop/main/staging: usar public (comportamiento legacy)
          elif [ "${{ github.ref_name }}" = "develop" ] || [ "${{ github.ref_name }}" = "main" ] || [ "${{ github.ref_name }}" = "staging" ]; then
            DB_SCHEMA="public"
            echo "schema=public" >> "$GITHUB_OUTPUT"
            echo "DB_SCHEMA=public" >> "$GITHUB_ENV"
          # Para workflow_dispatch (feature branches): usar branch_<slug> (sin SHA)
          else
            BRANCH_SLUG=$(echo "${{ github.event.inputs.branch || github.ref_name }}" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/-/g' | cut -c1-30)
            DB_SCHEMA="branch_${BRANCH_SLUG}"
            echo "schema=branch_${BRANCH_SLUG}" >> "$GITHUB_OUTPUT"
            echo "DB_SCHEMA=branch_${BRANCH_SLUG}" >> "$GITHUB_ENV"
          fi
          echo "DB_SCHEMA set to: $DB_SCHEMA"

      - name: Auth GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Configure Docker auth
        run: gcloud auth configure-docker "${{ vars.GCP_REGION }}-docker.pkg.dev" --quiet

      - name: Build image
        run: |
          image_uri="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ vars.ARTIFACT_REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"
          docker build -t "$image_uri" .

      - name: Push image
        run: |
          image_uri="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ vars.ARTIFACT_REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"
          docker push "$image_uri"

      - name: Deploy to Cloud Run
        run: |
          image_uri="${{ vars.GCP_REGION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ vars.ARTIFACT_REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"
          DB_SCHEMA="${{ steps.set_schema.outputs.schema }}"
          # Cloud Run usa PORT, pero la app espera HTTP_SERVER_PORT
          # Mapear PORT a HTTP_SERVER_PORT para compatibilidad
          ENV_VARS="DB_SCHEMA=${DB_SCHEMA},HTTP_SERVER_PORT=8080"
          if [ "${{ env.DEPLOY_ENV }}" = "prod" ]; then
            gcloud run deploy "${{ env.SERVICE_NAME }}" \
              --project="${{ env.GCP_PROJECT_ID }}" \
              --region="${{ vars.GCP_REGION }}" \
              --image="$image_uri" \
              --service-account="${{ env.CLOUD_RUN_SERVICE_ACCOUNT }}" \
              --update-env-vars="$ENV_VARS" \
              --no-allow-unauthenticated
          else
            gcloud run deploy "${{ env.SERVICE_NAME }}" \
              --project="${{ env.GCP_PROJECT_ID }}" \
              --region="${{ vars.GCP_REGION }}" \
              --image="$image_uri" \
              --service-account="${{ env.CLOUD_RUN_SERVICE_ACCOUNT }}" \
              --update-env-vars="$ENV_VARS" \
              --allow-unauthenticated
          fi
          echo "Deployed with DB_SCHEMA=${DB_SCHEMA}, HTTP_SERVER_PORT=8080"